/**
 * Type definition for subcontractor.
 *
 * This type defines the structure of a single subcontractor object.
 * @interface TSubcontractor
 */
export interface TSubcontractor {
  // Add fields as needed
}