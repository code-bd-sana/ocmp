/**
 * Type definition for training-toolbox.
 *
 * This type defines the structure of a single training-toolbox object.
 * @interface TTrainingToolbox
 */
export interface TTrainingToolbox {
  // Add fields as needed
}